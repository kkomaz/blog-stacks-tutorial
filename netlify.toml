[build]
  command = "yarn build"
  publish = "build"

# Specific branch context: all deploys from this specific branch will inherit
# these settings.
[context.staging] # 'staging' is a branch name
  OPEN_NODE_API_URL="Staging Environment"
